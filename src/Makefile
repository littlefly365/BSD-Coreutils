IMPDIR = libcompat libutil

SUBDIR =  arch base64 basename cat chmod chown chroot cksum comm cp csplit cut date dd df
SUBDIR += dirname du echo env expand expr false fmt fold head hexdump hostname id install 
SUBDIR += join kill link ln logname ls mkdir mkfifo mknod mktemp mv nice nl nohup nproc paste 
SUBDIR += pathchk printenv printf pwd realpath renice rm rmdir seq sleep sort split stat 
SUBDIR += sync tail tee test timeout touch tr true truncate tty uname unexpand uniq unlink
SUBDIR += users wc who yes

.include "../mk/subdir.mk"
